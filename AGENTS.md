# report-tool

브라우저 캔버스로 서류 양식(급여명세서·계약서)을 직접 설계하고, DB 데이터를 결합해
발행·배포·전자서명까지 처리하는 **임베더블 자바스크립트 라이브러리**.

특정 서비스에 종속되지 않으며, 호스트 애플리케이션에 붙여서 사용한다.

- 제품 상세: [docs/PRODUCT.md](docs/PRODUCT.md)
- 코드 구조: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)

---

## 개발 규칙 (예외 없음)

### 1. TypeScript + OOP
- 모든 코드는 TypeScript로 작성한다. `any` 금지.
- 로직은 클래스에 담는다. 떠도는 유틸 함수를 남발하지 않는다.
- 동작이 여러 갈래로 나뉘면 `if/switch` 나열 대신 **다형성**으로 푼다.

### 2. 가독성 우선
- 한 메서드는 한 가지 일만 한다. 20줄을 넘으면 쪼갠다.
- 이름은 줄이지 않는다. `tpl` 대신 `template`, `el` 대신 `element`.
- 영리한 한 줄보다 지루한 세 줄을 택한다.

### 3. 한글 주석 의무
- **모든 클래스·메서드 위에 그 역할을 설명하는 한글 주석을 단다.**
- 무엇을 하는지가 아니라 **왜 존재하는지**를 쓴다.
- 형식은 JSDoc(`/** ... */`)을 사용한다.

### 4. Layered Architecture
```
presentation  →  application  →  domain
infrastructure  →  application  →  domain
```
- **의존은 항상 안쪽(domain)을 향한다.** 역방향 import는 금지다.
- `domain`은 어떤 외부 패키지도 import하지 않는다. 브라우저 API도 Node API도 모른다.
- 바깥 세계(DB·파일·네트워크·캔버스)는 `application/port`의 인터페이스로만 만난다.

### 5. 과한 구조 금지
- 레이어는 이 4개가 전부다. 더 쪼개지 않는다.
- 사용하는 패턴은 5개로 제한한다: **Visitor, Strategy, Command, Port&Adapter, Factory**.
- 이벤트 소싱·CQRS·DI 컨테이너는 쓰지 않는다. 생성자 주입으로 충분하다.
- 추상화는 두 번째 구현체가 실제로 필요해질 때 만든다.

---

## 절대 바꾸지 않는 결정

| 결정 | 이유 |
|---|---|
| 좌표 단위는 **mm** | px로 저장하면 PDF·인쇄에서 어긋난다 |
| `schemaVersion` 필드 유지 | 마이그레이션의 유일한 근거 |
| 캔버스 라이브러리 직렬화 결과를 저장하지 않음 | 라이브러리 버전 올릴 때 과거 문서가 깨진다 |
| 발행 시 데이터·템플릿·PDF해시 **동결** | "무엇에 서명했는가"를 증명하는 근거 |
| 라이브러리는 I/O를 직접 하지 않음 | 급여 데이터가 고객사 밖으로 나가면 안 된다 |

## 검증된 기술 제약

- `@pdf-lib/fontkit`의 `subset: true`는 **한글 글리프를 누락시킨다**(재현 확인).
  반드시 harfbuzz(`subset-font`)로 미리 서브셋한 뒤 `subset: false`로 임베딩한다.
- 한글 폰트는 **OTF가 아닌 TTF**를 쓴다. OTF(CFF 아웃라인)는 임베딩 중 예외가 발생한다.

## 명령어

```bash
npm run poc:pdf     # 한글 PDF 렌더 PoC 실행
```

---

## 학습형 구현 진행 규칙

이 프로젝트의 구현 목적에는 사용자의 유지보수 역량 확보가 포함된다. 사용자가 별도의
세부 범위 없이 "구현해줘", "계속 구현해줘"처럼 요청하면 다음 절차를 항상 적용한다.

### 1. 한 번에 한 Phase만 구현

- [docs/TASKS.md](docs/TASKS.md)와 [docs/LEARNING_PROGRESS.md](docs/LEARNING_PROGRESS.md)를 읽고
  아직 완료되지 않은 가장 낮은 Phase를 찾는다.
- 한 요청에서 해당 Phase만 구현한다. 사용자가 명시적으로 범위를 지정하면 그 범위를 우선한다.
- 현재 Phase의 검증과 학습 확인이 끝나기 전에 다음 Phase를 먼저 구현하지 않는다.
- Phase가 너무 크면 `TASKS.md`의 작업 번호 단위로 나누되, 이번에 구현할 범위를 먼저 알린다.

### 2. 테스트를 먼저 작성

- 자동 테스트가 가능한 작업은 `TASKS.md`의 완료 조건을 실패하는 테스트로 먼저 옮긴다.
- 실패를 확인한 뒤 테스트를 통과하는 최소 구현을 작성하고, 마지막에 구조를 정리한다.
- 브라우저 상호작용처럼 자동화하지 않은 항목은 구체적인 수동 검증 절차와 확인 결과를 남긴다.
- 구현 종료 전에 관련 테스트, TypeScript 검사, 필요한 빌드를 직접 실행한다.

### 3. 구현 후 학습용 인계

Phase 구현이 끝나면 사용자에게 다음을 설명한다.

- 이번 Phase가 존재하는 이유와 전체 실행 흐름에서의 위치
- 생성·수정한 주요 클래스의 책임과 의존 관계
- 데이터가 들어와 결과로 나가는 흐름
- 작성한 테스트가 보장하는 동작과 아직 보장하지 않는 동작
- 사용자가 특히 이해해야 할 TypeScript, React, Konva 또는 아키텍처 개념
- 실제로 실행한 검증 명령과 결과

코드를 나열하는 설명보다 "왜 이 책임이 여기에 있는가"를 우선한다.

### 4. 사용자 실습을 통과해야 다음 Phase로 이동

- 현재 Phase를 조금 변경하는 작고 구체적인 실습 하나를 제안한다.
- 실습은 사용자가 직접 코드를 수정할 수 있을 만큼 작아야 하고, 성공 여부를 테스트로 확인할 수
  있어야 한다.
- 사용자가 요청하지 않는 한 실습의 완성 코드를 먼저 작성하지 않는다. 우선 힌트와 리뷰를 제공한다.
- 사용자가 실습을 완료하면 테스트와 코드를 검토하고, 통과했을 때만 해당 Phase의 학습 상태를
  완료로 기록한다.
- 실습을 생략하고 다음으로 진행하겠다고 사용자가 명시하면 그 결정을 따른다.

### 5. 진행 상태 기록

- 구현과 학습 상태를 [docs/LEARNING_PROGRESS.md](docs/LEARNING_PROGRESS.md)에 갱신한다.
- `구현 완료`와 `학습 확인 완료`를 별도로 기록한다.
- 새 세션에서는 이 기록을 기준으로 이어서 진행하되, 실제 파일과 테스트 결과도 함께 확인한다.
- 문서의 체크 표시만 보고 완료로 간주하지 않는다.

기본 반복 흐름은 다음과 같다.

```text
AI가 한 Phase 구현 및 검증
  → 구조와 테스트 설명
  → 사용자가 질문
  → 사용자가 작은 변경 수행
  → AI가 테스트와 변경을 검토
  → 학습 상태 기록
  → 다음 Phase 진행
```
